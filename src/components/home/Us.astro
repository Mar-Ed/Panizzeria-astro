---
import PizzaBox from "../svg/PizzaBox.astro";
---

<section class="about-us-section">
  <div class="about-container wrapper">
    <div class="box">
      <div class="top-container">
        <div class="sticker-pizza">
          <PizzaBox />
        </div>
        <div class="title-container">
          <h1 class="title">
            Nuestra historia comienza con la obsesión de encontrar la masa
            perfecta
          </h1>
        </div>
      </div>

      <div class="first-box">
        <div class="img-container">
          <img
            class="img-box"
            src="/img/dueños.jpg"
            alt="Imagen de la pizzería"
          />
        </div>
        <div class="container-text">
          <h1 class="title-2">EL COMIENZO</h1>
          <h3 class="subtitle">
            En La Panizzería combinamos el arte del pan artesanal con la pasión
            por la auténtica pizza italiana. Fundada en 2017 por una pareja de
            italianos, Giulia Bianchi y Marco Rossi, nuestra pizzería nace con
            el sueño de compartir el verdadero sabor de Italia en las playas de
            Pulpos, Perú.
          </h3>
        </div>
      </div>
      <div class="first-box">
        <div class="container-text-2">
          <h1 class="title">LA CLAVE DE NUESTRO ÉXITO</h1>
          <h3 class="subtitle">
            El secreto del éxito reside en los mejores ingredientes para
            elaborar una pizza excepcional. Hoy buscamos la perfección, tal como
            lo hemos hecho durante 15 años, cuando abrimos la pizzería en Lima,
            Perú.
          </h3>
        </div>
        <div class="img-container">
          <img
            class="img-box"
            src="/img/plato-principal.jpg"
            alt="Imagen de la pizzería"
          />
        </div>
      </div>
      <div class="first-box">
        <div class="img-container">
          <img
            class="img-box"
            src="/img/sala.jpg"
            alt="Imagen de la pizzería"
          />
        </div>
        <div class="container-text">
          <h1 class="title-2">MISIÓN</h1>
          <h3 class="subtitle">
            Nuestra misión es ofrecer una experiencia gastronómica cercana y
            memorable, donde cada cliente se sienta en casa. Creemos en la
            cocina de corazón, en la calidez del servicio y en el compromiso con
            la comunidad local de Pulpos. Por eso trabajamos con productores de
            la región para que cada ingrediente llegue fresco a tu mesa.
          </h3>
        </div>
      </div>
      <div class="first-box">
        <div class="container-text-2">
          <h1 class="title">VISIÓN</h1>
          <h3 class="subtitle">
            Nuestra visión es ser el punto de encuentro para los amantes del pan
            y la pizza, innovando siempre con nuevas combinaciones de sabores y
            adaptándonos a las tendencias sin perder la esencia artesanal.
            Queremos que La Panizzería sea sinónimo de calidad, cercanía y
            creatividad.
          </h3>
        </div>
        <div class="img-container">
          <img
            class="img-box"
            src="/img/promos.jpg"
            alt="Imagen de la pizzería"
          />
        </div>
      </div>
    </div>
  </div>

  <div class="order-banner">
    <div class="order-container wrapper">
      <h2 class="order-title">¡Ordena tu pizza favorita y recíbela en casa!</h2>
      <a href="/Menu" class="button button-accent">Ordena aquí</a>
    </div>
  </div>
</section>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  window.addEventListener("load", () => {
    // Header Animation
    const headerTl = gsap.timeline({
      scrollTrigger: {
        trigger: ".top-container",
        start: "top 90%",
      }
    });

    headerTl.from(".sticker-pizza", {
      scale: 0,
      rotation: -180,
      duration: 1,
      ease: "back.out(1.7)"
    })
    .from(".title-container .title", {
      x: 50,
      opacity: 0,
      duration: 1,
      ease: "expo.out"
    }, "-=0.5");

    // Alternating Boxes Animation
    const boxes = gsap.utils.toArray(".first-box");
    
    boxes.forEach((box, i) => {
      const img = box.querySelector(".img-container");
      const text = box.querySelector(".container-text, .container-text-2");
      const isEven = i % 2 === 0;

      const tl = gsap.timeline({
        scrollTrigger: {
          trigger: box,
          start: "top 85%",
          toggleActions: "play none none none"
        }
      });

      tl.from(img, {
        x: isEven ? -60 : 60,
        opacity: 0,
        duration: 1.2,
        ease: "expo.out"
      })
      .from(text.children, {
        y: 30,
        opacity: 0,
        duration: 0.8,
        stagger: 0.2,
        ease: "expo.out"
      }, "-=0.8");
    });

    // Order Banner Animation
    gsap.from(".order-banner", {
      scrollTrigger: {
        trigger: ".order-banner",
        start: "top 90%",
      },
      y: 50,
      opacity: 0,
      duration: 1,
      ease: "power3.out"
    });
  });
</script>

<style>
  .about-us-section {
    display: flex;
    flex-direction: column;
    padding-top: 100px; /* Base height of fixed navbar */
    background-color: var(--body-color);
  }
  .order-banner {
    width: 100%;
    padding: 1.5rem 0 0.1rem;
    background-color: transparent;
    border-top: 1px solid rgba(0,0,0,0.05);
  }

  .order-container {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 3rem;
  }

  .order-title {
    color: var(--nav-color);
    font-family: var(--second-font);
    font-size: 1.5rem;
    margin: 0;
    letter-spacing: 0;
    line-height: normal;
  }

  .button-accent {
    background-color: var(--first-color) !important;
    color: white !important;
    padding: 0.8rem 2rem !important;
    font-size: 0.9rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    border-radius: 0.5rem !important;
    transition: all 0.4s ease;
    box-shadow: 0 5px 15px rgba(255, 99, 71, 0.2);

    &:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(255, 99, 71, 0.3);
    }
  }

  @media (max-width: 768px) {
    .order-container {
      flex-direction: column;
      gap: 1.5rem;
      text-align: center;
    }
  }

  .about-container {
    text-align: center;
    width: 100%;
  }
  .top-container {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 4rem;
    position: relative;
    background-color: var(--nav-color);
  }
  .sticker-pizza {
    height: 8rem;
    width: 8rem;
    display: flex;
  }
  .title-container {
    box-sizing: border-box;
    display: flex;
    justify-content: left;
    width: calc(66.6667%);
    flex-direction: column;
    font-family: var(--second-font);
  }
  .title {
    margin-top: 40px;
    margin-bottom: 40px;
    font-size: 48px;
    line-height: 56px;
    color: var(--body-color);
  }
  .title-2 {
    margin-top: 40px;
    margin-bottom: 40px;
    font-size: 48px;
    line-height: 56px;
    color: var(--nav-color);
  }

  .subtitle {
    font-size: var(--normal-font-size);
    font-family: var(--body-font);
    font-weight: 500;
    margin-bottom: 30px;
    text-align: justify;
  }

  .button {
    background-color: #ff6347;
    color: white;
    padding: 10px 20px;
    border-radius: 5px;
    text-decoration: none;
    transition: background-color 0.3s ease;
  }

  .button:hover {
    background-color: #e55347;
  }

  .about__sticker {
    fill: var(#eba824);
  }

  .first-box {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
  }
  .img-container {
    box-sizing: border-box;
    max-width: 50%;
    vertical-align: bottom;
    display: block;
  }
  .img-box {
    width: auto;
    height: 100%;
    max-width: 100%; /* Adjust as needed */
  }
  .container-text {
    box-sizing: border-box;
    background-color: var(--container-color);
    max-width: 50%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 2rem;
  }
  .container-text-2 {
    box-sizing: border-box;
    background-color: var(--nav-color);
    color: var(--body-color);
    max-width: 50%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 3rem;
  }
  @media (max-width: 768px) {
    .about-us-section {
      padding-top: 70px; /* Reduced navbar height on mobile */
    }
    .order-banner {
      padding: 2rem 1rem;
      margin-bottom: 2rem;
    }
    .order-container {
      flex-direction: column;
      text-align: center;
      gap: 1.5rem;
    }
    .order-title {
      font-size: 1.5rem;
      text-align: center;
    }
    .top-container {
      flex-direction: column;
      gap: 1rem;
    }
    .title-container {
      width: 100%;
      text-align: center;
    }
    .title,
    .title-2 {
      font-size: 2rem;
      margin-top: 20px;
      margin-bottom: 20px;
      line-height: normal;
    }

    .first-box {
      flex-direction: column;
    }

    /* Target blocks where text comes first in HTML (container-text-2) and reverse them so image is on top */
    .first-box:has(.container-text-2:first-child) {
      flex-direction: column-reverse;
    }

    .img-container,
    .container-text,
    .container-text-2 {
      max-width: 100%;
      width: 100%;
    }
  }
</style>
