<section>
  <div class="container">
    <h1 class="title">La Panizzeria</h1>
    <p class="subtitle">
      Desde 1985, La Panizzeria ha sido el hogar de las mejores pizzas
      artesanales, combinando tradición italiana con sabores locales únicos.
    </p>
    <img
      src="/img/foto_principal_2.jpg"
      alt="Pizza Hero "
      class="hero-image"
    />
    <br />
    <a href="#" class="button">Descubre nuestras pizzas</a>
  </div>
</section>
<style>
  section {
    padding-top: 4rem; /* py-16 */
    padding-bottom: 4rem;
    padding-left: 1rem; /* px-4 */
    padding-right: 1rem;
    background-color: var(--container-color); /* bg-gray-100 */
  }
  .container {
    max-width: 64rem;
    display: column;
    text-align: center;
    align-items: center;
    justify-content: center;
    margin: 0 auto; 
  }
  
  .title {
    font-size: 2.25rem; /* text-4xl */
    font-weight: 700; /* font-bold */
    margin-bottom: 1.5rem; /* mb-6 */
    color: #1f2937; /* text-gray-900 */
  }
  @media (min-width: 768px) {
  .title {
    font-size: 3rem; /* md:text-5xl */
  }
}
  .subtitle {
    font-size: 1.25rem; /* text-xl */
  color: #4b5563; /* text-gray-600 */
  line-height: 1.625; /* leading-relaxed */
  }
  .hero-image {
    width: 100%;
    height: auto;
    max-width: 600px; /* max-w-md */
    margin-top: 1.5rem; /* mt-6 */
    border-radius: 0.5rem; /* rounded-lg */
    padding-bottom: 1rem;
  }
  .button {
    display: inline-block;
    padding: 0.75rem 1.5rem; /* py-3 px-6 */
    background-color: var(--nav-color); /* bg-orange-500 */
    color: var(--body-color); /* text-white */
    border-radius: 9999px; /* rounded-full */
    text-decoration: none; /* no underline */
    font-weight: 600; /* font-semibold */
  }
</style>
